# AI explanations and quiz generation (free key: aistudio.google.com)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional: persistent "saved elements" via Airtable.
# Without these, the app falls back to browser localStorage.
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE=Saves
